name: Pack & Publish (Stable by Tag)

on:
  push:
    tags:
      - 'utilities-*-v*'   # e.g., utilities-core-v1.0.0
  workflow_dispatch: {}

permissions:
  contents: read
  packages: write

jobs:
  pack-publish:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
        with: { fetch-depth: 0 }

      - uses: actions/setup-dotnet@v4
        with: { dotnet-version: '9.0.x' }

      - name: Restore
        run: dotnet restore

      - name: Build & Test
        run: |
          dotnet build -c Release --no-restore
          dotnet test -c Release --no-build

      - name: Determine project from tag
        id: proj
        shell: pwsh
        run: |
          $tag = "${{ github.ref_name }}"
          if ($tag -notmatch '^utilities-(.+)-v(.+)$') { throw "Bad tag: $tag (expected utilities-<key>-v<version>)" }
          $name = $Matches[1].ToLowerInvariant()
          $version = $Matches[2]
          $map = @{
            "core"          = "src/EagleViewEnt.Utilities/Core/EagleViewEnt.Utilities.Core.csproj"
            "efcore"        = "src/EagleViewEnt.Utilities/EFCore/EagleViewEnt.Utilities.EFCore.csproj"
            "logging"       = "src/EagleViewEnt.Utilities/Logging/EagleViewEnt.Utilities.Logging.csproj"
            "types"         = "src/EagleViewEnt.Utilities/Types/EagleViewEnt.Utilities.Types.csproj"
            "data"          = "src/EagleViewEnt.Utilities/Data/EagleViewEnt.Utilities.Data.csproj"
            "external"      = "src/EagleViewEnt.Utilities/External/EagleViewEnt.Utilities.External.csproj"
            "localization"  = "src/EagleViewEnt.Utilities/Localization/EagleViewEnt.Utilities.Localization.csproj"
            "mapping"       = "src/EagleViewEnt.Utilities/Mapping/EagleViewEnt.Utilities.mapping.csproj"
            "media"         = "src/EagleViewEnt.Utilities/Media/EagleViewEnt.Utilities.Media.csproj"
            "security"      = "src/EagleViewEnt.Utilities/Security/EagleViewEnt.Utilities.Security.csproj"
            "serialization.json" = "src/EagleViewEnt.Utilities/Serialization/Json/EagleViewEnt.Utilities.Serialization.Json.csproj"
            "serialization.xml" = "src/EagleViewEnt.Utilities/Serialization/Xml/EagleViewEnt.Utilities.Serialization.Xml.csproj"
            "windows"       = "src/EagleViewEnt.Utilities/Windows/EagleViewEnt.Utilities.Windows.csproj"
          }
          if (-not $map.ContainsKey($name)) { throw "Unknown package key: $name" }
          "proj=$($map[$name])" | Out-File -FilePath $env:GITHUB_OUTPUT -Append
          "version=$version"   | Out-File -FilePath $env:GITHUB_OUTPUT -Append

      - name: Pack (Release)
        run: dotnet pack "${{ steps.proj.outputs.proj }}" -c Release -o artifacts /p:Version=${{ steps.proj.outputs.version }}

      - name: Upload package artifact
        uses: actions/upload-artifact@v4
        with:
          name: nupkg
          path: artifacts/*.nupkg

      - name: Push to GitHub Packages
        shell: pwsh
        run: |
          $source = "https://nuget.pkg.github.com/EagleViewEnt/index.json"
          Get-ChildItem artifacts -Filter *.nupkg | ForEach-Object {
            dotnet nuget push $_.FullName --api-key ${{ secrets.GITHUB_TOKEN }} --source $source --skip-duplicate
          }
          Get-ChildItem artifacts -Filter *.snupkg | ForEach-Object {
            dotnet nuget push $_.FullName --api-key ${{ secrets.GITHUB_TOKEN }} --source $source --skip-duplicate
          }
